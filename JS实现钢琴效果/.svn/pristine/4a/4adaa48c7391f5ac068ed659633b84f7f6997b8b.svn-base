const getters = {
  addRouters: state => state.permission.addRouters,
  routers: state => state.permission.routers,
  token: state => state.permission.token,
  sidebar: state => state.app.sidebar,
  device: state => state.app.device,
  avatar: state => state.user.avatar,
  name: state => state.user.name,
  userInfo: state => state.user.userInfo,
  roles: state => state.user.role,
  departmentTree: state => state.attendance.departmentTree,
  entMemberTree: state => state.attendance.entMemberTree,
  appMainSize: state => state.app.contentSize,
  shiftContent: state => state.attendance.shiftContent
}
export default getters
